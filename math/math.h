#ifndef RESONANCE_MATH_H
#define RESONANCE_MATH_H

#include <stdint.h>

/* First argument is how many values follow. min(2, a, b).
 */
uint64_t min(uint64_t count, ...);

#endif /* RESONANCE_MATH_H */
